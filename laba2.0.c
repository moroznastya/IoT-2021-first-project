#include <stdio.h>
#include <math.h>

int main()
{
	printf("task 1/n");
	for (double x = 0.4; x <= 0.8; x += 0.2)

	{
		double y;
		if (x < 0.5)
		{
			y = pow(abs((log(x)), 7);
			printf("y=%lf/n", y);

		}
		if (x >= 0.5 && x > 0.7)
		{
			y = 1 / (tan(x + pow(x, 3));
			printf("y=%lf/n", y);
		}
		if (x >= 0.7)
		{
			y = log((sin(x)), 5);
			printf("y=%lf/n", y);
		}
	}

	printf("Task2/n")

		for (double x = 0.1, x <= 0.5, x += 0.001)
		{






		}


	return 0
}
